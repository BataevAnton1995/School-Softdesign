<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- Base application theme. -->


    <!-- Fragment Profile font-style for List DATA -->
    <style name="font">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="font.medium" parent="font">
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@string/ff_regular</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/font_large</item>
    </style>

    <style name="font.regular" parent="font">
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@string/ff_regular</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">@dimen/font_normal</item>
    </style>

    <style name="profile_strip">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@android:color/darker_gray</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:layout_marginLeft">@dimen/profile_text_margin</item>
    </style>

    <style name="toolbar_expanded_title">
        <item name="android:textSize">@dimen/toolbar_rating_size</item>
        <item name="android:textStyle">bold</item>
        <!-- Text Outline -->
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">0.0</item>
        <item name="android:shadowDy">0.0</item>
        <item name="android:shadowRadius">3.0</item>
    </style>
</resources>